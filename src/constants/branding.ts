/**
 * 브랜드 로고 정적 경로입니다.
 * - Next.js 에서 `public` 폴더의 파일은 웹 주소가 `/파일명` 으로 열립니다.
 * - 로그인·회원가입·온보딩 등에서 같은 이미지를 씁니다.
 * - 실제 그림 파일: `public/logo.png` (브랜드 3D 마크 PNG)
 */
export const AUTH_LOGO_SRC = '/logo.png' as const
