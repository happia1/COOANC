/**
 * 부모 앱(`/parent/home` 등) 탭·페이지 전환 시 잠깐 보이는 로딩 UI 입니다.
 */

import ParentSegmentLoadingClient from '@/components/parent/ParentSegmentLoadingClient'

export default function ParentSegmentLoading() {
  return <ParentSegmentLoadingClient />
}
